{
  "name": "patros-clone",
  "version": "1.0.0",
  "description": "A Patros clone from themewagon template",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "tailwindcss -i ./src/css/styles.css -o ./dist/styles.css --watch",
    "build": "tailwindcss -i ./src/css/styles.css -o ./dist/styles.css"
  },
  "devDependencies": {
    "tailwindcss": "^3.3.5"
  },
  "dependencies": {
    "preline": "^2.0.2"
  }
}
